---
scope: KFC Kiosk Recommendation System
purpose: build-substrate
altitude: feature
updated: 2026-07-06T08:42
---

- (event) User chose Coaching path
- (decision) Deliverables: ARCHITECTURE-SPINE.md for developer-judges, and a high-level pitch presentation for non-technical audience
- (constraint) Constraint: 3-day hackathon timeline for backend, frontend, and backtest
- (constraint) Constraint: Must run fully locally with simple local deployment
- (constraint) Constraint: GenAI calls limited to 1 call per recommendation request (low latency, low cost)
- (constraint) Constraint: No physical POS or kiosk hardware integration required
- (version) Stack: Python 3.12, FastAPI, SQLite, pandas, mlxtend, vanilla HTML/JS/CSS, Google Gemini API
- (decision) Design Paradigm: Pipes and Filters for core recommendation pipeline, wrapped in standard Layered structure
- (decision) Storage: Use JSON/CSV files loaded into memory for quick lookup; defer SQLite migration as a post-MVP step
- (decision) GenAI Client: Primary copy generator calls Google Gemini API. Fallback: Local rule-based templates if API fails or is slow. Defer: Ollama local Llama 3.2 integration as a secondary provider if API speed tests fail during development.
- (decision) Online Reranker: Use multiplicative scoring formula combining base confidence, promo boost, and time of day boost. Defer: Re-evaluate and refine the scoring formula after the initial end-to-end flow is implemented.
- (decision) API Contracts: /api/recommend returns list of recommended items with name, price, score, rationale, and copy. /api/backtest returns backtest metrics (AOV baseline vs hybrid, absolute/percentage uplift).
- (decision) Directory Structure: main.py in root, static/ for frontend UI, data/ under _bmad-output/data/
- (event) spine finalized
